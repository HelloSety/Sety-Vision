<section class="section container" style="padding-top:0">
  <div class="about-grid">
    {%- if section.settings.image -%}
      {{ section.settings.image | image_tag: width: 300, alt: section.settings.heading, loading: 'lazy', class: 'about-logo-img' }}
    {%- else -%}
      <img class="about-logo-img" src="{{ 'about-model.jpg' | asset_img_url: '300x' }}" alt="{{ section.settings.heading }}" width="300" height="300" loading="lazy">
    {%- endif -%}
    <div>
      <h2>{{ section.settings.heading | default: 'O que é a Fist Street' }}</h2>
      <p>{{ section.settings.text }}</p>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Sobre a Loja",
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Imagem" },
    { "type": "text", "id": "heading", "label": "Título", "default": "O que é a Fist Street" },
    { "type": "textarea", "id": "text", "label": "Texto", "default": "A Fist Street nasceu da cultura de rua: sneakers, moletons, jaquetas e peças com atitude para quem vive o streetwear de verdade. Curadoria própria, qualidade que se sente e estilo que representa." }
  ],
  "presets": [{ "name": "Sobre a Loja" }]
}
{% endschema %}
