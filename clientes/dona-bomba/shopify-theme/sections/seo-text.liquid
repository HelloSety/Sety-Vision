<div class="sr-only">
  <h2>{{ section.settings.heading }}</h2>
  {{ section.settings.text }}
</div>

{% schema %}
{
  "name": "Texto de SEO (oculto)",
  "settings": [
    { "type": "text", "id": "heading", "label": "Título" },
    { "type": "richtext", "id": "text", "label": "Texto" }
  ],
  "presets": [{ "name": "Texto de SEO (oculto)" }]
}
{% endschema %}
