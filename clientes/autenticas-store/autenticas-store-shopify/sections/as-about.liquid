{{ 'autenticas-store.css' | asset_url | stylesheet_tag }}

<div class="color-{{ section.settings.color_scheme }} gradient as-about-section">
  <div class="page-width">
    <div class="as-about-grid">
      <img class="as-about-logo" src="{{ section.settings.image_file | asset_url }}" alt="{{ shop.name }}" loading="lazy" width="200" height="200">
      <div>
        <h2>{{ section.settings.heading }}</h2>
        <div class="as-about-text">{{ section.settings.text }}</div>
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Sobre (logo do tema)",
  "tag": "section",
  "class": "section-as-about",
  "settings": [
    {
      "type": "text",
      "id": "image_file",
      "label": "Nome do arquivo em Assets",
      "default": "autenticas-store-logo.png"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Título",
      "default": "O que é a Autênticas Store"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Texto"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Esquema de cor",
      "default": "scheme-2"
    }
  ],
  "presets": [
    {
      "name": "Sobre (logo do tema)"
    }
  ]
}
{% endschema %}
