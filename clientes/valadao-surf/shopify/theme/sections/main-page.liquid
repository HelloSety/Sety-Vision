<section class="section container" style="max-width:800px">
  <h1 style="font-size:28px;font-weight:900;margin-bottom:24px;text-transform:uppercase">{{ page.title }}</h1>
  <div style="color:var(--cinza);line-height:1.8;font-size:15px">{{ page.content }}</div>
</section>
