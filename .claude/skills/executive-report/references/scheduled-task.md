# Scheduled task — report recorrente

Apos validar o report, o usuario pode agendar a execucao recorrente (ex.: toda segunda 7h).
Regra: mostre a configuracao (horario, pipeline, destinatario) para APROVACAO antes de criar a automacao. Nunca crie scheduled task sem confirmar.
